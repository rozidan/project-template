spring:

  jpa:
    show-sql: true

    hibernate:
      ddl-auto: create-drop

logging:
  level:
    root: info
    com.company.template: debug

flyway:
  enabled: false

levelog:
  rest:
    enabled: false