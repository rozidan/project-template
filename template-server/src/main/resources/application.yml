spring:

  jpa:
    show-sql: true

    hibernate:
      ddl-auto: create-drop
      use-new-id-generator-mappings: true
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyComponentPathImpl

  mvc:
    throw-exception-if-no-handler-found: true

  jackson:
    default-property-inclusion: non_null
    serialization.write-dates-as-timestamps: false

server:
  context-path: /template
  port: 8080

security:
  basic:
    enabled: false

logging:
  level:
    root: info
    com.company.template: debug

flyway:
  enabled: false

levelog:
  kafka:
    enabled: true


spring.boot.admin:
  url: http://mng:8080/mng/
  client.health-url: http://template:8080/template/mng/health/
  client.management-url: http://template:8080/template/mng/
  client.service-url: http://template:8080/template/

management:
  context-path: /mng
  security.enabled: false